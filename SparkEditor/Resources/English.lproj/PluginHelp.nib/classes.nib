{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {helpView = id; selectPlugin = id; }; 
            CLASS = PluginHelpController; 
            LANGUAGE = ObjC; 
            OUTLETS = {helpView = WebView; popupMenu = NSPopUpButton; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}