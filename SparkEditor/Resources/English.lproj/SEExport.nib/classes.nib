{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = SEExportOptions; 
            LANGUAGE = ObjC; 
            OUTLETS = {uiGroup = NSPopUpButton; uiIcons = NSButton; }; 
            SUPERCLASS = SKViewController; 
        }, 
        {
            CLASS = SKViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {view = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}