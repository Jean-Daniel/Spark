{
    IBClasses = (
        {
            ACTIONS = {
                exportList = id; 
                exportPrintable = id; 
                newGroup = id; 
                newKey = id; 
                revertDocumentToBackup = id; 
                saveAsArchive = id; 
                toggleApplications = id; 
                toggleServer = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {showPlugInHelp = id; showPreferences = id; }; 
            CLASS = Spark; 
            LANGUAGE = ObjC; 
            OUTLETS = {aboutMenu = NSMenu; statusMenuItem = NSMenuItem; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}